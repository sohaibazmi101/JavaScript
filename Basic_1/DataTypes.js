/*
A large of data type we will use in web and software development journey
*/


//  boolean tells true or fales
//  int x = 10; its an iteger value

// typeof is used find the type of datatypes

var x = 100;
let name = "Sohaib Azmi";
var Boolean, isActive = true;

console.table([x, name, isActive]);

console.table([typeof x, typeof name, typeof isActive]);    // It is used to print the table form of multiple variable

/*

Possible outputs are 

┌─────────┬───────────────┐
│ (index) │ Values        │
├─────────┼───────────────┤
│ 0       │ 100           │
│ 1       │ 'Sohaib Azmi' │
│ 2       │ true          │
└─────────┴───────────────┘
┌─────────┬───────────┐
│ (index) │ Values    │
├─────────┼───────────┤
│ 0       │ 'number'  │
│ 1       │ 'string'  │
│ 2       │ 'boolean' │
└─────────┴───────────┘
*/